# Copy to .env for local runs (never commit .env).
# Hugging Face Space: add the same names under Settings → Secrets / Variables.

GROQ_API_KEY=your_groq_api_key_here
HF_TOKEN=your_hf_read_or_write_token_here
