# Runtime deps for Hugging Face Spaces (Docker + JupyterLab).
# Keep this lean and stable so the Space builds reliably.

# JupyterLab (used by start_server.sh)
jupyterlab>=4.2,<5
jupyter-server>=2.14,<3

# API and Environment
groq>=0.31.0
python-dotenv>=1.0.0
huggingface_hub>=0.23.0

# Visualization / analytics
pandas>=2.2
plotly>=5.22
numpy>=1.26
scipy>=1.12
matplotlib>=3.7
pyyaml>=6.0
